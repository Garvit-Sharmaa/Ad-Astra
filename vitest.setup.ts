// vitest.setup.ts
// This adds useful assertions like .toBeInTheDocument() to Vitest's `expect`
// Fix: Use the /vitest entrypoint for @testing-library/jest-dom v6+
import '@testing-library/jest-dom/vitest';
